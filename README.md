# KUDI SKUNK Frontend (Vite + React)

## Run
1. Start backend on http://localhost:4010
2. In this folder:
   - `npm install`
   - `npm run dev`
3. Open http://localhost:5173

## Notes
- This is **mock-payment** UI only (no extra features).
- Backend base URL is hard-coded: `http://localhost:4010`.
